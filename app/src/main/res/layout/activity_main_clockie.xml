<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/color_fafafa"
    tools:context=".MainClockieActivity">
    <!-- The content of screen(container) which will store the WorldClock Screen, StopWatch, Alarm-->
    <FrameLayout
        android:id="@+id/content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    <!-- The content of SideMenu View which will store the side menu item -->
    <ScrollView android:id="@+id/sideMenuView"
        android:layout_width="@dimen/side_menu_view_width"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/color_fefefe"/>
</android.support.v4.widget.DrawerLayout>
